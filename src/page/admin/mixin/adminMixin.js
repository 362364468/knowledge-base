export const adminMixin = {
	// beforeRouteEnter(to, from, next) {
	// 	let name = to.path;
	// 	name = name.substring(name.lastIndexOf('/') + 1)
	// 	sessionStorage.setItem("keepName", name);
	// 	next();
	// },
	// mounted() {
	// 	let name = sessionStorage.getItem("keepName");
	// 	this.$store.commit('addKeepAliveRouter', name);
	// }
}